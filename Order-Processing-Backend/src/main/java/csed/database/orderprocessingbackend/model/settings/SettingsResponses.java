package csed.database.orderprocessingbackend.model.settings;

public enum SettingsResponses {
    INVALID_SESSION_ID, FAILED, EMAIL_ALREADY_TAKEN, USERNAME_ALREADY_TAKEN, EMAIL_ALREADY_EXISTS, SUCCESSFUL
}
